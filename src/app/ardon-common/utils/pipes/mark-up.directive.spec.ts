import { MarkUpDirective } from './mark-up.directive';

describe('MarkUpDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkUpDirective();
    expect(directive).toBeTruthy();
  });
});
